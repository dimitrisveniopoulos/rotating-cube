# Cube Rotation in OpenGL

This in an OpenGL project in C that demonstrates cube rotation.

Run RotatingCube.exe (use freeglut.dll file found in repo if needed).

Change rotation centre through right-click menu.
